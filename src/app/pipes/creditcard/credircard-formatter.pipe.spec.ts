import { CredircardFormatterPipe } from './credircard-formatter.pipe';

describe('CredircardFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CredircardFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
